# SatAI — SatQuery AI · runtime requirements
fastapi>=0.110.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0
numpy>=1.26
scipy>=1.11
Pillow>=10.0
matplotlib>=3.8
pydantic>=2.7
python-multipart>=0.0.9
python-dotenv>=1.0
tifffile>=2024.1.30

# --- optional: local VLM serving + RS fine-tuning (training extra) ---
# torch>=2.2  transformers>=4.49  peft>=0.13  accelerate>=1.0
# bitsandbytes>=0.44  datasets>=3.0  qwen-vl-utils>=0.0.8
# rasterio>=1.3   # GeoTIFF deep-probe (tifffile covers the basic path)
